Config = {}

-- Quantité par item quand on fait /givelist (ne fonctionne pas pour les armes)
Config.ItemCount = 10

-- Nom du stash temporaire (label affiché en haut de l'inventaire)
Config.StashLabel = 'OLDGIVE'


-- Mode auto-remplissage (give infini)
Config.AutoFill = true

-- Avtier les logs (console)
Config.printLog = true

-- N'oubliez pas d'ajouter la commande dans votre .cfg
-- add_ace group.owner givelist.use allow (owner uniquement pour les owner)
